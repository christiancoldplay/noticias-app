import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zschristian.noticiasapp',
  appName: 'noticias-app',
  webDir: 'www'
};

export default config;
